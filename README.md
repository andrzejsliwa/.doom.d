# My ~/.doom.d for Doom Emacs

# Install

``` sh
git clone git@github.com:andrzejsliwa/.doom.d.git ~/.doom.d
```

``` sh
git clone --depth 1 https://github.com/doomemacs/doomemacs ~/.emacs.d
~/.emacs.d/bin/doom install
```

